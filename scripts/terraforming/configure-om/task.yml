---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfinfrastructure/releng-tools

inputs:
- name: terraform-state-gcp
- name: ci

run:
  path: ci/scripts/terraforming/configure-om/task

params:
  GCP_SERVICE_ACCOUNT_KEY:
  OM_USERNAME:
  OM_PASSWORD:
