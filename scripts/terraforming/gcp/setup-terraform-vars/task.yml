---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfinfrastructure/releng-tools

inputs:
- name: terraforming-gcp
- name: ci
- name: opsman-image

outputs:
- name: tfvars

run:
  path: ci/scripts/terraforming/gcp/setup-terraform-vars/task

params:
  GCP_PROJECT_ID:
  GCP_SERVICE_ACCOUNT_KEY:
  GCP_SSL_CERT:
  GCP_SSL_PRIVATE_KEY:
