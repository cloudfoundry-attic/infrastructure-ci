---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfinfrastructure/deployment

inputs:
  - name: etcd-cf-env
  - name: diego-release
  - name: infrastructure-ci
  - name: stemcell

run:
  path: infrastructure-ci/scripts/etcd/deploy-diego/task

params:
  BOSH_DIRECTOR:
  BOSH_USER:
  BOSH_PASSWORD:
