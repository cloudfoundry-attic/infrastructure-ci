---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfinfrastructure/deployment

inputs:
  - name: cf-release
  - name: etcd-cf-env
  - name: etcd-release
  - name: infrastructure-ci
  - name: stemcell

run:
  path: infrastructure-ci/scripts/etcd/deploy-cf/task

params:
  BOSH_ENVIRONMENT:
  BOSH_CLIENT:
  BOSH_CLIENT_SECRET:
  BOSH_CA_CERT:
