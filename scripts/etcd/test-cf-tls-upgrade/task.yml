---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfinfrastructure/deployment

inputs:
  - name: infrastructure-ci
  - name: etcd-release

run:
  path: infrastructure-ci/scripts/etcd/test-cf-tls-upgrade/task

params:
  CF_DOMAIN:
  CF_USER:
  CF_PASSWORD:
  BOSH_DIRECTOR:
  BOSH_USER:
  BOSH_PASSWORD:
  DEPLOYMENT_NAME:
