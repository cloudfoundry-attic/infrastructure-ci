---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfinfrastructure/deployment

inputs:
  - name: mega-ci
  - name: oss-s3-buckets-stack
  - name: release-repo
  - name: release-repo-master

outputs:
  - name: final-release-repo

run:
  path: ci/scripts/create-final-release/task

params:
  RELEASE_NAME:
