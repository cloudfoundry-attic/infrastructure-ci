---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfinfrastructure/deployment

inputs:
  - name: ci
  - name: bbl-states-repo

run:
  path: ci/scripts/delete-deployment/task

params:
  BBL_STATE_DIR:
  DEPLOYMENT_NAME:
