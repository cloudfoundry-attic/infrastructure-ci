---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfinfrastructure/deployment

inputs:
  - name: ci

run:
  path: ci/scripts/delete-deployments/task

params:
  BOSH_DIRECTOR:
  BOSH_USER:
  BOSH_PASSWORD:
  DEPLOYMENTS_WITH_WORD:
