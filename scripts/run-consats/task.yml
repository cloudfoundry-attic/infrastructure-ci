---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfinfrastructure/deployment

inputs:
  - name: ci
  - name: consul-release
  - name: stemcell
  - name: windows-stemcell
  - name: turbulence-release
  - name: bosh-aws-cpi-release
  - name: latest-consul-release

run:
  path: ci/scripts/run-consats/task

params:
  BOSH_DIRECTOR_CA_CERT:
  BOSH_PASSWORD:
  BOSH_DIRECTOR:
  BOSH_USER:
  PARALLEL_NODES:
  ENABLE_TURBULENCE_TESTS:
  WINDOWS_CLIENTS:
